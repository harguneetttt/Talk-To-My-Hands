###### Libraries used - 
-----> cv2 - python's computer vision library, accessing webcam, drawing shapes(circle, line), displaying video frames
cv2.VideoCapture()
cv2.circle()
cv2.line()
cv2.imshow()
cv2.waitKey()
input camera frame --> draw landmarks --> output frame


---> numpy - numerical computing library
store landmark coordinates,

---> Mediapipe - framework for doing real-time tasks
hand tracking
face detection
pose estimation
object detection
detecting hand and finger positions
mediapipe model detects 21 landmarks in this project (hand landmarking)

---> Tensorflow


---> from mediapipe.tasks import python
---> from mediapipe.tasks.python import vision
.. Because the hand detection model is created using: vision.HandLandmarker, This class is located inside the vision module.
Provides access to MediaPipe’s modern Tasks API.
mp.solutions ❌(old) mp.tasks ✅(new)
Interface for loading hand detection model

---> mp_hands = mp.tasks.vision.HandLandmarksConnections
Mediapipe store karta hai how each joint is connected to the other, basically it already knows via this line
it knows thumb joint -> thumb tip
I have not used this line in my code, reason...????

---> mp_drawing = mp.tasks.vision.drawing_utils
mediapipe knows how to draw landmarks as well, but we did manually with openCV
usecase...????

---> mp_drawing_styles = mp.tasks.vision.drawing_styles
Mediapipe has ready-made colours and styles for drawing the hand
not used again...????

---> HAND_CONNECTIONS
this is like manually telling which point connects to what (which landmark points should be connected with lines)
each (start_idx, end_idx) eg - (0,1) draw line from landmark 0 to landmark 1

0           wrist
1 2 3 4     thumb
5 6 7 8     index
9 10 11 12  middle
13 14 15 16 ring
17 18 19 20 pinky
if i see (7,8) meaning draw line between two joints of index finger

---> base_options = python.BaseOptions(model_asset_path='/Users/harguneet/Downloads/hand_landmarker.task')
tells mediapipe which AI model to use, pretrained machine learning model file by google
brain of hand detection basically
this model can look at the image and tell -- yeh hand location, these are 21 landmarks, 3D position of each landmark

---> options = vision.HandLandmarkerOptions(
    base_options = base_options,                        // use the model we just loaded
    num_hands = 2,                                      // number of hands
    running_mode = vision.RunningMode.VIDEO             // video mein frame-by-frame, can be photo/live webcam also
)
this line tells how our hand detector should behave
live stream mode used when frames are asynchronous (advanced streaming pipelines)

---> detector = vision.HandLandmarker.create_from_options(options)
it creates hand detection system that i defined earlier
maine mediapipe ko already bta diya maine konsa model use kiya, kitne hands detect karvaye, and video input format
now detector will take an image, and return hand landmarks, orientation, presence
pehle i just loaded instructions but detector is the main engine

---> cap = cv2.VideoCapture(1)          cap becomes the main camera object
1 is the device number, 0 par phone camera start hojaata tha
basically 0 default, 1 second and 2 third camera
used to access webcam, video files

---> timestamp = 0
a counter that increases every frame, mediapipe expects each frame must have one timestamp
timestamp allows mediapipe to track movement across frames

---> while True:
running the loop forever until a break condition occurs
capture -> process -> display

---> ret, frame = cap.read()
ret returns 2 outputs (True - frame captured successfully, False - nope)
frame is the actual image from camera (like a picture)

---> new_frame = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)
opencv BGR but mediapipe expects RGB, model was trained on RGB, toh might be detection fail hojaaye
numpy array ki form mein hai new_frame (wow)
new_frame = shape: (height, width, 3)
type: uint8
color order: RGB (because you converted it)

---> timestamp+=1 every frame gets a timestamp and mediapipe knows next frame hai

---> mp_image = mp.Image(image_format = mp.ImageFormat.SRGB, data = new_frame)
mediapipe does not understand raw numpy array, so it uses mp.image to know the color format, manage memory etc
Numpy arr -> Mediapipe object (conversion)
image_format is how the pixels are arranged (SRGB -> because we used RGB, GRAY8 -> grayscale images)

---> hand_landmarker_result = detector.detect_for_video(mp_image, timestamp) actual inference step
NORMALISATION OCCURS HERE BY DEFAULT --> this line returns normalised value instead of pixel values
normalized_x = pixel_x / image_width
normalized_y = pixel_y / image_height
runs in 2 stages (bounding box detection and hand landmark detection)
it sends the image into the handlandmarker model
image
 ↓
hand detection model
 ↓
hand bounding box
 ↓
hand landmark model
 ↓
21 landmark points
timestamp is important as well because of obv reasons
21 landmarks each having x,y,z which gives value which are Normalized (0 → left/top, 1 → right/bottom)
later converted into pixels by multiplying with frame height, width

---> CRITICAL SECTION
Now for every image/frame looking for 21 points per frame would be very slow, so model first detects the hand and
capture a hand bounding box. 
system focuses on that particular region. 
now next step of the model is to run neural networks only on this part to detect landmarks.
0           wrist
1 2 3 4     thumb
5 6 7 8     index
9 10 11 12  middle
13 14 15 16 ring
17 18 19 20 pinky
using this again, the model produces something like:
[
(x0,y0,z0),
(x1,y1,z1),
...
(x20,y20,z20)
]
these are 3D coordinates of the hand joints

so image -> 21 landmarks ❌
image
 ↓
hand detector
 ↓
hand region
 ↓
landmark model
 ↓
21 points ✅
faster processing, better accuracy

VIDEO ALLOWS OPTIMIZATION
frame 1 → detect hand
frame 2 → track previous hand
frame 3 → track again
no need for re-detection again and again, if hand goes out of frame, and later comes back again then re-detection
Landmarks are normalised because it fits each camera resolution
later converted into pixels by multiplying with frame height, width
z coordinate means closer/far away from camera with values being negative/positive respectively

---> if hand_landmarker_result.hand_landmarks:
v crucial step, this checks if hands exist or not, program crashes without it

---> for hand_landmarks in hand_landmarker_result.hand_landmarks:
when hands are detected (1 or 2) so loop through each

---> h, w, c = frame.shape (height, width, color channels = 3 represent RGB)

---> for lm in hand_landmarks:
each hand has 21 landmarks, so this loop will run 21 times
each iteration will have 1 lm and later converting each landmark into pixel format
x = int(lm.x * w)
y = int(lm.y * h)

---> cv2.circle(frame, (x, y), 12, (255, 213, 63), cv2.FILLED)
*** they expect an integer value that is why x,y have used int 
wherever the landmark is detected, draw a circle
frame → image to draw on
(x,y) → center of circle
12 → radius
(255,213,63) → color
cv2.FILLED → filled circle
RESULT TILL NOW : DOTS APPEARING ON HANDS FOR ALL 21 HAND Landmarks

---> for start_idx, end_idx in HAND_CONNECTIONS:
hand_landmarker is a list[
landmark0,
landmark1,
landmark2,
...
landmark20
]
x1 = int(hand_landmarks[start_idx].x * w)
y1 = int(hand_landmarks[start_idx].y * h)
x2 = int(hand_landmarks[end_idx].x * w)
y2 = int(hand_landmarks[end_idx].y * h)

looping through hand connections, loop runs through every connection pair, get pixel coordinates, and draws a line between them
start point → index joint
end point → index tip

---> cv2.line(frame, (x1, y1), (x2, y2), (255,0,0), 4)
frame → image
(x1,y1) → start point
(x2,y2) → end point
(255,0,0) → color
4 → thickness

---> cv2.imshow("Output", frame) Display the processed frame in a window.
Window name: Output, This window updates every loop iteration, creating video.

---> if cv2.waitKey(1) == ord('q'):
        break
check if user pressed "Q", Wait 1 millisecond for a key press.
ord('q') converts the character into its ASCII number.

---> cap.release() Frees the webcam device

cv2.destroyAllWindows() Closes any OpenCV windows that were created.